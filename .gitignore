__pycache__/
*.pyc
*.pyo

# Tracker secrets — never commit
tracker_config.json

# Local scan state — not needed on GitHub
scanned_files.json
watcher.log

# Local Resume/ subfolder (kept only on machine)
Resume/
